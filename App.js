import React, {Component} from 'react';
import {View,Text,FlatList,TouchableOpacity,} from 'react-native';
import AppContainer from "./src/screens/Navigator"

export default class App extends Component {
    render() {
        return (

            <AppContainer/>

        );
    }
}